// Use
// protoc --python_out=. research.proto
// to build this proto.

syntax = "proto3";

message Research {
  optional string query = 1;
  repeated string facts = 2;
  optional string source = 3;
}
